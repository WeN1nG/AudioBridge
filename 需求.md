# 需求文档

## 情景

当用户购入电脑但是没有购入音响时，电脑没有声音输出功能。本项目让用户可以在没有音响的情况下，通过 USB 连接手机等有扬声器的设备，将电脑音频转发到设备扬声器输出。

## 核心需求

1. 用户通过 USB 数据线连接 Android 设备和电脑。
2. 程序读取 USB 端的设备信息，判断设备是否有扬声器能力。
3. 如果设备有扬声器，程序将通过该设备的扬声器输出电脑声音。
4. 如果设备没有扬声器，程序提示用户无法使用该设备。

## 实现原理

程序自身通过 **Scream 虚拟音频驱动** 伪装成一个 Windows 音频输出设备，电脑将声音信号发送给该虚拟驱动。虚拟驱动通过 UDP 组播将 PCM 音频数据发出，C# 服务接收后通过 **ADB 端口转发 (adb forward tcp)** 将数据发送到 Android 设备上运行的 audio_player 进程，最终通过 **AAudio API** 输出到设备扬声器。

## 数据流

```
Windows 音频系统 → Scream虚拟驱动(UDP组播)
  → AudioBridge C# 服务(UDP接收)
  → ADB 端口转发 (TCP)
  → Android audio_player (AAudio)
  → 设备扬声器
```
