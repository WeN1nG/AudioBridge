# 需求文档

## 情景

当用户购入电脑但是没有购入音响时，电脑没有声音输出功能。本项目让用户可以在没有音响的情况下，通过 USB 连接手机等有扬声器的设备，将电脑音频转发到设备扬声器输出。

## 核心需求

1. 用户通过 USB 数据线连接 Android 设备和电脑。
2. 程序读取 USB 端的设备信息，判断设备是否有扬声器能力。
3. 如果设备有扬声器，程序将通过该设备的扬声器输出电脑声音。
4. 如果设备没有扬声器，程序提示用户无法使用该设备。

## 实现原理

程序通过 **WASAPI Loopback API** 从 PC 当前默认音频输出设备捕获音频流（PCM 格式），经格式转换（float→int、采样率转换、位深转换）后，通过 **ADB 端口转发 (adb forward tcp)** 将 PCM 数据发送到 Android 设备上运行的 audio_player 进程，最终通过 **AAudio API** 输出到设备扬声器。

## 前置条件

- PC 需至少有一个活动的音频输出设备（板载声卡、HDMI 音频、USB 声卡等），WASAPI Loopback 从该设备捕获混音后的音频
- Android 设备需开启 USB 调试

## 数据流

```
Windows 音频系统 (默认渲染设备)
  → WASAPI Loopback (进程内 COM API)
  → float→int 格式转换 (可选 SRC 采样率转换)
  → ADB 端口转发 (adb forward tcp:27777)
  → Android audio_player (AAudio TCP socket)
  → 设备扬声器
```
