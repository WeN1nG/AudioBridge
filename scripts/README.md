# 构建与运行脚本

## build_all.ps1

一键构建所有组件。

## build_driver.ps1

编译虚拟音频驱动（需要 Visual Studio + WDK，目前使用预编译驱动）。

## install_driver.ps1

安装 Scream 虚拟音频驱动（需要管理员权限）。

## run_bridge.ps1

启动 AudioBridge 服务。

## kill_audio_bridge.ps1

终止 AudioBridge 服务。
